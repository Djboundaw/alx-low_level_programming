#include "main.h"

/**
 * rot13 - function that encodes a string using rot12
 * @s: * before s, the string to encode
 *
 * Return: return the result of the encodage
 */
char *rot13(char *s)
{
	return (s);
}
