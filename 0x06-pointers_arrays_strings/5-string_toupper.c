#include "main.h"

/**
 * string_toupper - function that changes all
 * lowercase letters to uppercase
 * @s: * before s, the string
 *
 * Return: return the new string
 */
char *string_toupper(char *s)
{
	return (s);
}
