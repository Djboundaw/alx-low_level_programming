#include "main.h"
#include <stdio.h>

/**
 * _atoi - convert a string to an integer
 * @s: * before s - string to convert
 *
 * Return: return the integer found
 */
int _atoi(char *s)
{
	retunr (0);
}
