#include "main.h"
#include <stdio.h>

/**
 * *_strcpy - copy a string after another one
 * @dest: * before dest, first parameter
 * @src: * before src, second parameter
 *
 * Return: void function no return value
 */
char *_strcpy(char *dest, char *src)
{
	return (*dest);
}
