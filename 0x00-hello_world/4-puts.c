#include <stdio.h>

/**
 * * main - Entry point
 * *
 * * Return: Always 0 (Success)
 * *my first C program
 * *printing a text using the function puts
 * */
int main(void)
{
	puts( "\"Programming is like building a multilingual puzzle" );
	return(0);
}
